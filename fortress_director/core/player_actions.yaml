---
- id: repair_wall
  label: "Reinforce Wall"
  requires:
    - structure_id
  safe_function: "reinforce_wall"
  args_map:
    structure_id: structure_id

- id: move_npc
  label: "Move NPC"
  requires:
    - npc_id
    - x
    - y
  safe_function: "move_npc"
  args_map:
    npc_id: npc_id
    x: x
    y: y

- id: assign_patrol
  label: "Assign Patrol"
  requires:
    - npc_id
    - duration
  safe_function: "send_on_patrol"
  args_map:
    npc_id: npc_id
    duration: duration
