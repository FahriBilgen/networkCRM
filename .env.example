# Fortress Director environment defaults

# LLM çalışma modu: llm (canlı modeller) veya stub (offline/fallback).
FORTRESS_LLM_MODE=llm

# Gerçek LLM testlerini CI veya yerel koşulda tetiklemek için 1 yapın.
FORTRESS_RUN_LLM_TESTS=0

# API servis portu (api.py üzerinden FastAPI).
FORTRESS_API_PORT=8000

# Log yapılandırması.
FORTRESS_LOG_DIR=logs
FORTRESS_LOG_CONSOLE_LEVEL=INFO
FORTRESS_LOG_FILE_LEVEL=WARNING
# Opsiyonel, özel log dosyası yolunu belirtir.
FORTRESS_LOG_PATH=logs/fortress.log

# Ollama host adresi (gerekiyorsa override edin).
OLLAMA_HOST=http://localhost:11434/
